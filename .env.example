# 复制为 .env 并填入真实值（不要提交真实密钥）
# 供应商：mock(免Key验证) / tongyi(通义千问VL) / deepseek(DeepSeek-VL)
LLM_PROVIDER=mock

# 通义千问（选 tongyi 时必填）
DASHSCOPE_API_KEY=sk-xxxx

# DeepSeek（选 deepseek 时必填）
DEEPSEEK_API_KEY=sk-xxxx

# 对外端口
CLOUD_PORT=5000
