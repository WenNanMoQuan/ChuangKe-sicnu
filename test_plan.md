# 测试与验证方案（阶段七：系统验证与交付定型）

> 对应规划：阶段七。覆盖功能测试、性能测试、稳定性测试、实地场景测试。

## 一、功能测试
1. **盲道引导状态转换路径**（用 `test_fsm.py` 验证算法，真机补测）：
   - 跟随 → 偏移 → 纠正 → 跟随
   - 跟随 → 转弯 → 跟随
   - 跟随 → 终止
   - 偶发误识别（光变/阴影）被正确防抖过滤
2. **障碍物三级预警组合**：分别构造 1/2/3 级及混合障碍，验证震动 PWM、蜂鸣、灯色映射正确。
3. **播报优先级**：3 级障碍触发时，盲道语音被压制；同类型语音间隔 ≥1s。

## 二、性能测试
- 用 200 张样本图统计识别准确率（盲道≥85%，障碍≥80%）。
- 端到端延迟：拍照时刻 → 反馈执行时刻的时间差，要求 ≤3s。
- 帧率与功耗监测（阶段六低功耗策略生效时帧率降至 0.5FPS，空闲关摄像头）。

## 三、稳定性测试
- 满电全功能持续运行 ≥8 小时，记录崩溃次数与看门狗恢复次数（应为 0 崩溃）。
- 弱网/断网场景：验证自动切换离线超声波兜底，网络恢复自动回在线。

## 四、实地场景测试
- 校园内至少 3 条不同盲道路线真机实测，记录识别结果与用户体验。
- 覆盖逆光、暗光、磨损、遮挡、雨天等复杂场景（对应 V3.0 提示词）。

## 五、回归与交付
- 修复所有问题后回归验证；冻结最终软硬件版本并打 tag。
- 导出 TF 卡运行日志（CSV）做分析，产出《TF卡运行日志分析报告》。
- 交付物：定型原型机、全套源码、硬件文档、使用手册、测试报告、演示 PPT/视频。

## 六、自动化工具
- `test_client.py`：向 08 云端服务发送测试图，验证 `/api/recognize` 联通与返回格式。
- `test_fsm.py` + `fsm_model.py`：状态机防抖逻辑单元测试（pytest）。
